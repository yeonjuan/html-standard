import { attributes } from "../common/attributes";
import { ElementSpec } from "../types";

export const br: ElementSpec = {
  contents: {
    model: null,
  },
  attributes: attributes.globalOnly,
};
