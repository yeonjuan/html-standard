import { attributes } from "../common/attributes";
import { ElementSpec } from "../types";

export const cite: ElementSpec = {
  attributes: {
    global: true,
    specific: attributes.empty,
  },
};
